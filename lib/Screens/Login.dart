// ignore_for_file: unnecessary_const

import 'package:flutter/material.dart';

class Login extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Material(
      child: const Center(
        child: Text("dhtdhgfjhfg"),
      ),
    );
  }
}
